# Storage backend: "local" (default) or "googledrive"
# STORAGE_BACKEND=local

# --- Local storage ---
# Path to Tesla dashcam folder (required when STORAGE_BACKEND=local)
# e.g., /mnt/usb/TeslaCam, /Volumes/TESLA/TeslaCam, ~/GoogleDrive/teslacam
TESLACAM_PATH=/path/to/your/teslacam/folder

# --- Google Drive storage ---
# Path to credentials JSON file (required when STORAGE_BACKEND=googledrive)
# Supports both OAuth credentials (rclone-style) and service account keys.
# The file should contain a root_folder_id field, or set GOOGLE_DRIVE_FOLDER_ID below.
# GOOGLE_DRIVE_CREDENTIALS_FILE=./google-drive-secrets.json

# Override root folder ID (optional, defaults to root_folder_id from credentials file)
# GOOGLE_DRIVE_FOLDER_ID=

# Server port (optional, default: 3001)
# PORT=3001
